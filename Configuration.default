#!/bin/sh

#
#  Configuration.default
#

if [ "${MGICONFIG}" = "" ]
then
    MGICONFIG=/usr/local/mgi/live/mgiconfig
    export MGICONFIG
fi

. ${MGICONFIG}/master.config.sh

# change as needed for your build. See build.config.default for details.
. ./build.config.default
