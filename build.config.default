# where to find web templates and GlobalConfig.properties
WEB_RESOURCES=${MGI_LIVE}/mgiconfig/web

# which web xml file to use
WEB_XML=./config/web.xml.public

# where to deploy the war file
DEPLOY_DIR=${WILDFLY}/fewi/deployments/

# if you want a second deployment (for a batch instance) define the path
DEPLOY_BATCH_DIR=
